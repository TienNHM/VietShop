<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/home"
        android:contentDescription="@string/accountDescription"
        android:icon="@drawable/home"
        android:title="@string/home"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/service"
        android:contentDescription="@string/pay"
        android:icon="@drawable/service"
        android:title="@string/pay"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/notifications"
        android:contentDescription="@string/notificationsDescription"
        android:icon="@drawable/notifications"
        android:title="@string/notifications"
        app:showAsAction="ifRoom" />

</menu>